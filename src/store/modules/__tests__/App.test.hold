// import {
//   createLocalStore
// } from '@/utils/local_store'
// const localStore = createLocalStore()

// import {
//   getters,
//   mutations,
//   actions
// } from '../App.js';


import * as app from '../App.js'

describe('mutations', () => {
  it('SET_CURRENT_ENTRY set current entry to state', () => {
    const currentEntry = 'main'
    const state = {
      currentEntry: ''
    }
    mutations.SET_CURRENT_ENTRY(state, currentEntry)
    expect(state.currentEntry).toBe(currentEntry)
  })
})


// const mutations = {
//   SET_CURRENT_ENTRY(state, payload) {
//     state.currentEntry = payload
//   },
//   SET_CURRENT_PAGE(state, payload) {
//     state.currentPage = payload
//   }
// }
